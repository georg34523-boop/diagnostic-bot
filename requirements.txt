aiogram==3.4.1
supabase==2.0.0
python-dotenv==1.0.1
aiofiles==23.2.1
fastapi==0.109.0
uvicorn==0.27.0
aiohttp==3.9.3

# anyio закріплена НАВМИСНО.
# 02.09.2026 вийшла anyio 4.15.0, у якій більше не працює звертання
# anyio.abc без явного імпорту. httpcore (його тягне supabase через httpx)
# оновлювався востаннє у квітні 2025 і саме так до нього і звертається:
#   File "httpcore/_backends/anyio.py", line 20
#     def __init__(self, stream: anyio.abc.ByteStream) -> None:
#   AttributeError: module 'anyio' has no attribute 'abc'
# Через це 04.09.2026 сервіс не піднявся після звичайного деплою:
# образ пересібрався і підтягнув свіжу anyio.
# 4.14.2 — остання версія, на якій усе працювало.
# Знімати закріплення лише після перевірки, що httpcore оновився.
anyio==4.14.2
